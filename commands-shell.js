// Components

$ mongod // The databases process
$ mongos // Sharding constroller
$ mongo // The database shell

// Utilities

$ mongodump // Dump tool - for backups, snapshots, etc
$ mongorestore // Restore a dump
$ mongoexport // Export a single collection to test (JSON, CSV)
$ mongoimport // Import from JSON or CSV
$ mongofiles // Utility for putting and gettings files from MongoDB GridFS
$ mongostat // Show performace statistics

// $ aggregate
// $ db.collection.aggregate()
// 